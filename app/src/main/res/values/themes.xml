<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Material3, DayNight, ohne alte NoActionBar-Varianten -->
    <style name="Theme.FitApp" parent="android:Theme.Material3.DayNight">
        <!-- Edge-to-Edge: transparente Systemleisten; Kontrast steuern wir in MainActivity -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- CJK/RTL/FontScale etc. bleiben Material3-Defaults -->
        <!-- Falls du eine Start-Splash brauchst, später eine theme_splash anlegen -->
    </style>
</resources>
