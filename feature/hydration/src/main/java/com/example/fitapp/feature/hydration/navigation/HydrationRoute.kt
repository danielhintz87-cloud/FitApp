package com.example.fitapp.feature.hydration.navigation

/**
 * Navigation routes for the Hydration feature module.
 * These define the destinations available within the hydration feature.
 */
object HydrationRoute {
    const val HOME = "hydration_home"
    const val GOALS = "hydration_goals"
    const val HISTORY = "hydration_history"
}